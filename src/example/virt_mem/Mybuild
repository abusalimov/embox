package embox.example.vmem

module mmuprobe {
	source "mmu_probe.c"

	depends embox.mem.vmem.vmem
	depends embox.compat.libc.all
	depends embox.compat.posix.util.getopt
	depends embox.framework.example.example
}
